version: '2'

syncs:
  api-volume:
    src: '.'
    sync_host_port: 10871
    sync_excludes: ['tmp', 'log', 'vendor', '.git', '.bundle']
options:
  compose-file-path: 'docker-compose.yml'
  compose-dev-file-path: 'docker-compose-with-sync.yml'

